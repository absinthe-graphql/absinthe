defmodule Absinthe.Specification.Introspection.Schema.NonNullTest do
  use ExSpec, async: true
  import AssertResult

  describe "introspection of an object type that includes a non-null" do

    @tag :pending
    it "can use __type and see fields with the wrapping non-null types" do
    end

  end

end
